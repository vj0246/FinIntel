# Production backend (Render). Vite auto-loads this during `vite build`, so the
# deployed frontend always points at the live API. A Vercel dashboard env var of
# the same name would override this if you ever change it.
VITE_API_BASE=https://mumpy-demo.onrender.com
